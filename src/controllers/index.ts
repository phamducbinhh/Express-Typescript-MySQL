const authController = require('./auth.controllers')
const userController = require('./user.controller')
const categoryController = require('./categories.controllers')
const bookController = require('./books.controllers')

module.exports = {
  authController,
  userController,
  categoryController,
  bookController
}
