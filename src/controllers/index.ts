const authController = require('./auth.controllers')
const userController = require('./user.controller')
const insertController = require('./insert.controllers')

module.exports = {
  authController,
  userController,
  insertController
}
