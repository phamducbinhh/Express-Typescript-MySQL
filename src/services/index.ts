const authService = require('./auth.services')
const userService = require('./user.services')
const categoryService = require('./categories.services')

module.exports = { authService, userService, categoryService }
