const authService = require('./auth.services')
const userService = require('./user.services')
const insertService = require('./inster.services')

module.exports = { authService, userService, insertService }
